:root {
  --bg-1: #070b14;
  --bg-2: #101827;
  --panel: rgba(17, 24, 39, 0.8);
  --panel-strong: rgba(15, 23, 42, 0.96);
  --muted: #94a3b8;
  --soft: #e2e8f0;
  --gold: #fbbf24;
  --gold-strong: #f59e0b;
  --emerald: #34d399;
  --red: #f87171;
  --shadow: rgba(15, 23, 42, 0.65);
  --border: rgba(148, 163, 184, 0.18);
}

* {
  box-sizing: border-box;
}

html {
  scroll-behavior: smooth;
}

body {
  margin: 0;
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
  background:
    radial-gradient(circle at top, rgba(251, 191, 36, 0.18), transparent 25%),
    radial-gradient(circle at bottom right, rgba(52, 211, 153, 0.12), transparent 22%),
    linear-gradient(135deg, var(--bg-1), var(--bg-2));
  color: var(--soft);
  letter-spacing: 0.02em;
}

header {
  width: min(1100px, 90%);
  text-align: center;
  margin-top: 28px;
}

header h1 {
  margin: 0;
  font-size: clamp(2.3rem, 5vw, 4rem);
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: #f8fafc;
  text-shadow: 0 0 25px rgba(251, 191, 36, 0.32);
}

main {
  width: min(1100px, 90%);
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 18px;
  padding: 24px 20px 40px;
}

hr {
  width: 100%;
  border: none;
  height: 1px;
  background: linear-gradient(90deg, transparent, rgba(251, 191, 36, 0.7), transparent);
  margin: 8px 0 18px;
}

p {
  margin: 0;
}

#timer,
#start-typing-text,
#congrats,
#end-text,
#finish-time-text,
#net-wpm-text,
#raw-wpm-text,
#scoreboard {
  text-align: center;
}

#timer {
  font-size: 1.35rem;
  font-weight: 700;
  color: var(--gold);
}

#start-typing-text {
  margin-top: 8px;
  color: var(--muted);
  font-size: 1.1rem;
  font-style: italic;
}

.time-select-box,
.text-type-select-box {
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 12px;
  flex-wrap: wrap;
  background: rgba(15, 23, 42, 0.72);
  border: 1px solid var(--border);
  border-radius: 18px;
  padding: 12px 18px;
  box-shadow: 0 20px 40px rgba(0, 0, 0, 0.22);
  backdrop-filter: blur(10px);
}

.time-buttons,
.text-type-buttons,
#restart-button {
  font-family: inherit;
  border: 1px solid transparent;
  transition: all 0.2s ease;
  cursor: pointer;
}

.time-buttons,
.text-type-buttons {
  min-width: 72px;
  padding: 10px 14px;
  border-radius: 12px;
  background: rgba(148, 163, 184, 0.08);
  color: var(--muted);
  font-weight: 700;
  letter-spacing: 0.04em;
}

.time-buttons:hover,
.text-type-buttons:hover {
  color: var(--soft);
  border-color: rgba(251, 191, 36, 0.38);
  transform: translateY(-1px);
}

.time-buttons.active,
.text-type-buttons.active {
  background: linear-gradient(135deg, rgba(251, 191, 36, 0.18), rgba(245, 158, 11, 0.12));
  color: var(--gold);
  border-color: rgba(251, 191, 36, 0.5);
  box-shadow: inset 0 0 0 1px rgba(251, 191, 36, 0.3), 0 0 18px rgba(251, 191, 36, 0.12);
}

.text-type-buttons {
  min-width: 160px;
}

#text-container {
  width: min(1000px, 100%);
  min-height: 180px;
  background: rgba(15, 23, 42, 0.68);
  border: 1px solid rgba(148, 163, 184, 0.18);
  border-radius: 26px;
  padding: 28px 26px;
  box-shadow: 0 25px 60px rgba(2, 6, 23, 0.52);
  text-align: left;
  font-size: clamp(1.2rem, 2vw, 2.2rem);
  line-height: 1.8;
  letter-spacing: 0.02em;
  position: relative;
  overflow: hidden;
}

#text-container::before {
  content: "";
  position: absolute;
  inset: 0;
  background: linear-gradient(180deg, rgba(148, 163, 184, 0.04), transparent 30%, rgba(251, 191, 36, 0.03));
  pointer-events: none;
}

.word {
  display: inline-block;
  white-space: nowrap;
  margin-right: 0.4em;
  padding: 0.08em 0.04em;
  border-radius: 6px;
}

.letter {
  display: inline-block;
  position: relative;
  transition: color 0.12s ease, background 0.12s ease;
}

.letter.current {
  color: #f8fafc;
  background: rgba(251, 191, 36, 0.08);
  border-radius: 4px;
  box-shadow: inset 0 -2px 0 rgba(251, 191, 36, 0.8);
}

.letter.correct {
  color: var(--emerald);
}

.letter.incorrect {
  color: var(--red);
  background: rgba(248, 113, 113, 0.12);
  border-radius: 4px;
}

.letter.current-caret::before {
  content: "";
  position: absolute;
  left: -1px;
  top: 14%;
  width: 2.5px;
  height: 72%;
  background: linear-gradient(180deg, var(--gold), #fde68a);
  border-radius: 99px;
  box-shadow: 0 0 14px rgba(251, 191, 36, 0.8);
  animation: caretBlink 1s step-end infinite;
}

@keyframes caretBlink {
  0%, 50% { opacity: 1; }
  50.01%, 100% { opacity: 0; }
}

#hidden-text-input {
  position: absolute;
  left: -9999px;
  opacity: 0;
  pointer-events: none;
}

#restart-button {
  padding: 12px 26px;
  border-radius: 999px;
  background: linear-gradient(135deg, var(--gold), var(--gold-strong));
  color: #0f172a;
  font-weight: 900;
  font-size: 1rem;
  box-shadow: 0 12px 22px rgba(251, 191, 36, 0.26);
}

#restart-button:hover {
  transform: translateY(-2px) scale(1.01);
  box-shadow: 0 18px 28px rgba(251, 191, 36, 0.35);
}

#restart-button:active {
  transform: translateY(0);
}

footer {
  width: 100%;
  padding: 18px 12px 28px;
  text-align: center;
}

footer p {
  color: var(--muted);
  font-size: 0.95rem;
}

footer a {
  color: var(--gold);
  text-decoration: none;
  font-weight: 700;
}

footer a:hover {
  text-decoration: underline;
}

@media (max-width: 640px) {
  .time-select-box,
  .text-type-select-box {
    gap: 8px;
    padding: 10px 12px;
  }

  .time-buttons,
  .text-type-buttons {
    width: 100%;
    min-width: 0;
  }

  #text-container {
    padding: 20px 18px;
    border-radius: 20px;
    font-size: 1.1rem;
  }
}
